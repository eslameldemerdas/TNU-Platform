# EngHub — Engineering Student Platform

Unified academic & campus platform for engineering students.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env` and set the required values.
3. Run the app:
   `npm run dev`

## Build for Production

`npm run build`

`npm start`
